let table;
var circles = [];


function preload() {
  table = loadTable('party-data.csv', 'csv', 'header');
}

function setup() {
  createCanvas(windowWidth, windowHeight);
  noLoop();
}

function draw() {
  
}